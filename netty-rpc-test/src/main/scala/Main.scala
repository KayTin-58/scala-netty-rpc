
import scala.collection.mutable.ListBuffer

object Main {
  def main(args: Array[String]): Unit = {
    //    val array = new Array[Integer](2)
    //    array(0) =1
    //    Utils.test(array)

    val list = new ListBuffer[String]().+=("").+=("")

   list+= "KK"

    println(list.size)

    "a" match {
      case "a" =>
      case "a" =>
    }

  }
}
